export const metadata = {
  title: "XLSX",
  description: "Testes",
};

export default function RootLayout({ children }) {
  return (
    <html lang="pt-BR">
      <body>{children}</body>
    </html>
  );
}
